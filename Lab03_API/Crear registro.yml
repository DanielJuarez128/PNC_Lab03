info:
  name: Crear registro
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/specimen"
  body:
    type: json
    data: |-
      {
        "name":"Kraken bebe",
        "region":"Oceano",
        "dangerLevel":1,
        "isFriendly":true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​